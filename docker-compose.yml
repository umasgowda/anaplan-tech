version: "3.5"

services:

  mysql-db:
    image: mysql:5
    ports:
      - 3306:3306
    environment:
      MYSQL_ROOT_PASSWORD: test123
      MYSQL_DATABASE: definitions
    volumes:
      - ./database:/docker-entrypoint-initdb.d
    healthcheck:
      test: ["CMD-SHELL", "mysql -ptest123 -e 'select 1'"]
      interval: 5s
      retries: 50

  api:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 80:80
      - 443:443

  dashboards:
    image: umashivalingaiah2/dashboards:latest
    build: ./bring-your-own-interview
    ports:
        - 8081:8081
        - 1000:1000
    depends_on:
       mysql-db:
         condition: service_healthy

  prometheus:
    image: 'prom/prometheus:latest'
    volumes:
      - ./prometheus-config.yml:/etc/prometheus/prometheus.yml
    ports:
      - 9090:9090

